# This is a Databricks asset bundle definition for trvrda.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: trvrda
  uuid: c1cd98b0-68d1-4801-b5a0-27eeb64eb7ac

include:
  - resources/*.yml
  - resources/*/*.yml

# # Variable declarations. These variables are assigned in the dev/prod targets below.
# variables:
#   catalog:
#     description: The catalog to use
#   schema:
#     description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-16688268-4222.cloud.databricks.com
  prod:
    mode: production
    workspace:
      host: https://dbc-16688268-4222.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/vardhanxxv@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/vardhanxxv@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - group_name: admins
        level: CAN_MANAGE
